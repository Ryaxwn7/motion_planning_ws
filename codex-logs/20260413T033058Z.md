Time: 2026-04-13T03:30:58Z
Topic: Add an AMCL-based simtest startup script parallel to shape_assembly_5x10_d2.sh without changing the existing Gazebo odom entrypoint.

Decisions
- Kept the existing `shape_assembly_5x10_d2.sh` unchanged as the Gazebo-odom entrypoint.
- Added a wrapper script that reuses the existing simtest startup chain but switches the user config basename to an AMCL-specific config.
- Added a dedicated AMCL user config with `map_to_odom: "amcl"` and otherwise the same 5x10_d2 world, map, RViz, and robot placement as the Gazebo-odom simtest config.
- Marked the new wrapper script executable.

Files
- src/ros_motion_planning/scripts/shape_assembly_5x10_d2_amcl.sh
- src/ros_motion_planning/src/user_config/user_config_5x10_d2_amcl_simtest.yaml

Interfaces
- New script: `src/ros_motion_planning/scripts/shape_assembly_5x10_d2_amcl.sh`
- New config: `src/ros_motion_planning/src/user_config/user_config_5x10_d2_amcl_simtest.yaml`
- AMCL switch is controlled through `map_to_odom: "amcl"` in the new user config.

Validation
- `bash -n src/ros_motion_planning/scripts/shape_assembly_5x10_d2_amcl.sh` passed.
- `python src/ros_motion_planning/src/plugins/dynamic_xml_config/main_generate_simtest.py user_config_5x10_d2_amcl_simtest.yaml` passed.
- Verified the new YAML parses with `map_to_odom=amcl`.
- `source /opt/ros/noetic/setup.bash && source devel/setup.bash && roslaunch --files sim_env main_simtest.launch` passed and expanded `sim_env/launch/include/location/amcl.launch.xml`.

Outputs
- codex-logs/20260413T033058Z.md
- src/ros_motion_planning/scripts/shape_assembly_5x10_d2_amcl.sh
- src/ros_motion_planning/src/user_config/user_config_5x10_d2_amcl_simtest.yaml

Open Questions
- `start_robots_simtest.launch.xml` still shows `map_to_odom` default text as `gazebo` when inspected directly after generation, but `roslaunch --files sim_env main_simtest.launch` resolves the AMCL launch branch. Runtime launch behavior should still be checked in a full GUI simulation if exact TF source behavior must be confirmed visually.
