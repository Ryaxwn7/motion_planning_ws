Date: 2026-03-22T06:07:27Z
Topic: Local shape_images requirement on host and robots

Decisions
- Enforced the deployment rule that both host and each robot must keep a local `shape_images` directory.
- Clarified that runtime shape selection on robots follows host-published `ShapeTask.shape_type`; local launch `shape_type` is fallback only.

Changes
- Updated `shape_task_supervisor.py` to resolve `shape_library_root` locally and warn if host-side shape images are missing.
- Updated `shape_task_bridge.py` to resolve `shape_library_root` locally and warn if robot-side shape images are missing.
- Updated `shape_assembly_swarm.py` to resolve `shape_library_root` locally and log that runtime shape selection follows the host task topic.
- Updated `host_robot_code_split.md` and `shape_assembly_real_robot_protocol.md` to document the local shape-image storage requirement and host-authoritative runtime shape selection.

Validation
- `python3 -m py_compile` passed for `shape_task_supervisor.py`, `shape_task_bridge.py`, and `shape_assembly_swarm.py`.

Open Items
- Real deployment still needs file-sync discipline so each robot workspace keeps the same `shape_images` set as the host.
