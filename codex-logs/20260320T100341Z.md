Date/Time: 2026-03-20T10:03:41Z
Topic: Real-robot planner migration phase 1-3

Decisions:
- Executed the first three migration steps in the real-robot workspace:
- Replaced the old `fm2` implementation with the updated `new_ws` version.
- Migrated the updated `my_planner` implementation while preserving real-robot topic/frame conventions and conservative control defaults.
- Added `fm2star` as an optional global planner without changing the default planner selection.
- Kept real-robot interfaces such as `laser`, `odom_combined`, and `base_footprint`; did not import simulation-oriented `odom`, `laser_link`, or `base_link` defaults.
- Wrote a parameter comparison and migration rationale document to `planner_migration_param_compare.md`.

Validation:
- `my_planner` built successfully with `catkin_make_isolated --merge`.
- `graph_planner` and its dependency chain built successfully with `catkin_make_isolated --merge`.
- Full workspace `catkin_make` is still blocked by an unrelated missing `serial` package in `turn_on_wheeltec_robot`.

Open Questions / Next Steps:
- On hardware, compare `global_planner:=fm2` against `global_planner:=fm2star` for planning latency and path quality.
- If phase 1-3 is stable, continue with the next migration phase or start the distributed `shape_assembly` adaptation separately.
