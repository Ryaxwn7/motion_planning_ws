Time: 2026-04-13T02:19:05Z
Topic: Fix simtest gather startup deadlock caused by missing /combined_map publisher and verify /gather_signal=2 starts FM2 gather.

Decisions
- Kept the gather start trigger as `/gather_signal` with `std_msgs/UInt8(data=2)`; no algorithm change was needed.
- Identified the real blocker: simtest used `use_combined_map: true` but did not launch `fm2_map_combine_node`, so `/combined_map` had no publisher and `fm2_gather_node` never reached `Map_init`.
- Added `enable_map_combine: true` to the simtest orchestration path so future `shape_assembly_5x10_d2.sh` runs launch the map combiner automatically.
- Left the real-robot `start_robot.sh` chain untouched.

Files
- src/ros_motion_planning/src/sim_env/config/shape_assembly_simtest.yaml
- src/ros_motion_planning/src/sim_env/launch/shape_assembly_with_main_simtest.launch
- src/ros_motion_planning/scripts/shape_assembly_5x10_d2.sh

Interfaces
- Added simtest config key `enable_map_combine: true`.
- Added simtest launch arg `enable_map_combine` and forwarded it to `fm2_gather/launch/laun_simtest.launch`.
- `shape_assembly_5x10_d2.sh` now forwards YAML `enable_map_combine` into the simtest launch unless overridden on the command line.

Validation
- `rostopic info /combined_map` before fix showed `Publishers: None`.
- Started `rosrun fm2_gather fm2_map_combine_node` in the live ROS graph; node auto-detected 4 robots and subscribed to `/robotN/move_base/global_costmap/costmap(_updates)`.
- `rostopic info /combined_map` after starting map combine showed publisher `/fm2_map_combine_node`.
- Published `rostopic pub -1 /gather_signal std_msgs/UInt8 '{data: 2}'`; `rostopic echo -n 1 /gather_started` returned `data: 1`.
- `rostopic echo -n 1 /gather_center` returned a valid map-frame center `(1.975, -2.925)`.
- `bash -n src/ros_motion_planning/scripts/shape_assembly_5x10_d2.sh` passed.
- `source /opt/ros/noetic/setup.bash && source devel/setup.bash && roslaunch --files sim_env shape_assembly_with_main_simtest.launch agent_number:=4 use_fm2_gather:=true namespace_prefix:=robot` passed.

Outputs
- codex-logs/20260413T021905Z.md

Open Questions
- The currently running sim session was fixed live by starting `fm2_map_combine_node`; a fresh relaunch is still recommended so the new simtest defaults take effect cleanly from launch time.
