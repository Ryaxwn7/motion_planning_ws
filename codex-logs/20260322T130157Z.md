# 20260322T130157Z

- Topic: Real-robot shape assembly host experiment should use `/gather_signal` to trigger `fm2_gather` auto center computation, similar to `new_ws` script flow but without simulation.
- Decisions:
  - Rechecked current code and confirmed `fm2_gather` already supports `/gather_signal` value `2` to compute and publish gather center.
  - Reused existing `move_base_client/scripts/start_gather.py` instead of adding a new trigger node.
  - Added host-side script `src/ros_motion_planning/scripts/shape_assembly_real_robot.sh` to start `shape_assembly_host.launch`, optionally start `map_server`, and auto trigger `fm2_gather`.
  - Updated `deployment_checklist.md` to recommend the new real-robot host script and to describe the `/gather_signal -> fm2_gather -> /gather_center -> /shape_assembly/task` flow.
- Validation:
  - `bash -n src/ros_motion_planning/scripts/shape_assembly_real_robot.sh` passed.
  - Script executable bit set.
  - `roslaunch --files turn_on_wheeltec_robot shape_assembly_host.launch ...` passed.
- Open questions:
  - Real robots still need field validation for timing and whether host should start `map_server` or reuse an existing one in deployment.
