Time: 2026-04-13T04:24:38Z
Topic: Make the real-robot shape assembly odom suffix configurable from YAML instead of being hardcoded to `/odom`.

Decisions
- Removed the hardcoded real-robot `robot_odom_topic_suffix=/odom` override from the `formation_robot` launch chain.
- Kept launch-time override support by threading a `robot_odom_topic_suffix` arg through the real-robot wrapper launch files with `USE_YAML_SENTINEL` as the default.
- Restored YAML-first behavior so the real chain now uses `sim_env/config/shape_assembly.yaml` unless an explicit launch arg overrides it.

Files
- src/formation_robot/launch/formation_robot.launch
- src/turn_on_ws/src/turn_on_wheeltec_robot/launch/include/shape_assembly_agent.launch
- src/turn_on_ws/src/turn_on_wheeltec_robot/launch/motion_navigate_multi4.launch

Interfaces
- Added real-robot launch arg `robot_odom_topic_suffix` to `motion_navigate_multi4.launch`.
- Added real-robot launch arg `robot_odom_topic_suffix` to `shape_assembly_agent.launch`.
- Added real-robot launch arg `robot_odom_topic_suffix` to `formation_robot.launch`.
- Default value is `USE_YAML_SENTINEL`, so `shape_assembly.launch.xml` now falls back to `shape_assembly.yaml` for the real chain.

Validation
- `source /opt/ros/noetic/setup.bash && source devel/setup.bash && roslaunch --files formation_robot formation_robot.launch robot_namespace:=robot1 agent_id:=1 agent_number:=4 enable_shape_assembly:=true` passed.
- `source /opt/ros/noetic/setup.bash && source devel/setup.bash && roslaunch --files turn_on_wheeltec_robot motion_navigate_multi4.launch map_started:=true agent_number:=4 agent_id:=1 enable_shape_assembly:=true` passed.
- Checked the edited launch files with `nl -ba` to confirm the new arg is threaded through and no longer forces `/odom`.

Outputs
- codex-logs/20260413T042438Z.md

Open Questions
- Runtime topic verification on a real robot is still needed to confirm whether `/robotN/odom_combined` is the correct live odom source in your deployment.
- If you also want host-side gather/monitor suffixes to be YAML-driven from one central config, that needs a separate pass on `shape_assembly_host.launch` and its wrapper scripts.
