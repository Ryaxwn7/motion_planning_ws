Date/Time: 2026-03-20T09:41:46Z
Topic: Planner migration and distributed shape assembly migration assessment

Decisions:
- User clarified current workspace is the real-robot navigation/control workspace; `new_ws` is the simulation/algorithm workspace.
- Focus shifted from pruning to migration planning only; no code changes requested.
- Evaluated migration scope for updated `fm2`, `fm2star`, `sfmm2`, updated `my_planner`, and distributed `shape_assembly`.

Findings:
- Current real-robot launch chain is `motion_navigate_multi4.launch` with `fm2 + my + mini_mec`, using real-robot frames/topics such as `odom_combined`, `base_footprint`, and `laser`.
- `new_ws` planner stack adds `fm2star`, `sfmm2`, dynamic-reconfigure-enabled FM2 velocity-field tuning, dynamic obstacle uncertainty handling, and a newer `my_planner`.
- `new_ws` distributed `shape_assembly` exists and is implemented through `shape_assembly_swarm.py`, per-robot launch fan-out, and optional `fm2_gather` orchestration.
- Current workspace does not already contain `shape_assembly`; adding it would be an incremental new task stack, not a simple replacement.

Open Questions:
- Whether the real robots expose enough stable local costmap/TF data for distributed `shape_assembly`’s local obstacle avoidance as-is.
- Whether `fm2_gather` should be migrated together, or replaced by the existing real-robot navigation task flow.

Next Steps:
- Provide a no-code migration recommendation ranked by value, risk, and migration order.
