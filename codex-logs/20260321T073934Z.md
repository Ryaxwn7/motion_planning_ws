Date: 2026-03-21T07:39:34Z
Topic: Real-robot shape_assembly integration and host/robot mixed-mode control

Decisions
- Kept single ROS master on host and retained `fm2_gather`.
- Added distributed `shape_assembly` agent per robot with real-robot defaults (`/odom_combined`, `/cmd_vel`, `/move_base/cancel`).
- Added external shared-center input `/shape_assembly/center_goal_cmd` on host.
- Changed host replanning behavior to use per-robot `active_robot_ids` instead of globally stopping when any robot enters shape mode.
- Kept global `stop_path_planning` only as an optional all-robots-active stop condition.
- Added a short cancel grace period before shape agents publish non-zero velocity after takeover.

Implementation
- Copied `shape_assembly` assets/config/scripts into `sim_env`, plus local shape image library.
- Copied `fm2_gather` and `move_base_client` from `new_ws`.
- Patched `shape_assembly_swarm.py` for local shape library path, real-robot odom topic suffix, per-robot takeover reporting, and cancel grace period.
- Patched `fm2_gather` for external center input, per-robot active skip logic, real-robot namespace handling, and message-generation build dependencies.
- Patched `c_plan_monitor` to use configurable robot namespace/odom suffix and skip only active robots.
- Added `shape_assembly_host.launch` and `shape_assembly_agent.launch`.
- Extended `motion_navigate_multi4.launch` with optional shape-assembly integration switches.
- Wrote protocol and deployment notes to `shape_assembly_real_robot_protocol.md`.

Validation
- `python -m py_compile src/ros_motion_planning/src/sim_env/scripts/shape_assembly_swarm.py` passed.
- XML parsing passed for the new/modified launch files.
- Direct package build passed for `move_base_client`.
- Direct package build passed for `sim_env` dynamic reconfigure generation.
- Direct package build passed for `fm2_gather` after fixing missing target dependencies.
- Workspace-level `catkin_make` still fails on pre-existing missing `serial` package in `turn_on_wheeltec_robot`.

Next Steps
- Run first real integration test with 2 robots before 4+ robots.
- Verify `/shape_assembly/center_goal_cmd` dispatch and `/shape_assembly/active_robot_ids` updates on live robots.
- If needed, tune `cancel_grace_period`, `switch_reference_radius_margin`, and `r_sense` on the real platform.
