## Time
- 2026-03-22T04:28:29Z

## Topic
- Replace manual host `shape_heading` with the `new_ws`-style automatic heading selection based on available map space.

## Decisions
- Kept robot-side `shape_assembly` auto heading disabled; heading authority stays on the host.
- Extended `formation_host` so `shape_task_supervisor.py` now subscribes to `/map` and scores candidate headings using the same weighted shape-overlap logic used in `new_ws` `shape_assembly_swarm.py`.
- `shape_heading` remains as a fallback seed/manual override only when auto heading is disabled or the map/shape model is unavailable.
- Exposed `auto_shape_heading` and its main tuning params in `formation_host.launch`, `shape_assembly_host.launch`, and `motion_navigate_multi4.launch`.

## Validation
- `python3 -m py_compile src/formation_host/scripts/shape_task_supervisor.py` passed.
- Whitelist build passed again for `formation_msgs; formation_host; formation_robot; sim_env; fm2_gather; move_base_client`.
- `roslaunch --files turn_on_wheeltec_robot shape_assembly_host.launch agent_number:=4 auto_shape_heading:=true shape_type:=rectangle` passed.

## Open Questions
- Auto heading still assumes `/gather_center` and `/map` share the same frame; there is no TF conversion in the host node yet.
- `shape_scale` is carried in `ShapeTask` but is not yet used by the host heading optimizer or robot shape controller.
