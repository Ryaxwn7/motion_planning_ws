Date: 2026-03-22T11:09:40Z
Topic: Added host and robot build scripts

Decisions
- Added separate workspace-root build scripts for host-side and robot-side deployment.
- Used catkin whitelist builds to keep host and robot build scope aligned with deployment roles.

Changes
- Added `build_host.sh`.
- Added `build_robot.sh`.

Validation
- `bash -n` passed for both scripts.
- `--list` mode passed for both scripts.
- `build_host.sh` completed successfully.

Open Items
- `build_robot.sh` was syntax-checked and listed packages, but full robot build was not run here because robot-side environment may vary, especially the `serial` dependency for `turn_on_wheeltec_robot`.
