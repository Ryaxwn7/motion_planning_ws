# 20260322T133533Z

- Topic: Make root startup scripts config-driven instead of requiring manual arguments every run.
- Decisions:
  - Updated `start_host.sh` to read `config/host_start.conf` by default and allow CLI overrides.
  - Updated `start_robot.sh` to read `config/robot_start.conf` by default and allow CLI overrides.
  - Added `config/host_start.conf` and `config/robot_start.conf` as editable startup templates.
  - Updated `experiment_steps.md` and `deployment_checklist.md` to describe the config-file-driven workflow.
- Validation:
  - `bash -n start_host.sh` passed.
  - `bash -n start_robot.sh` passed.
  - `bash -n src/ros_motion_planning/scripts/shape_assembly_real_robot.sh` passed.
  - `roslaunch --files turn_on_wheeltec_robot shape_assembly_host.launch ...` passed.
  - `roslaunch --files turn_on_wheeltec_robot motion_navigate_multi4.launch ...` passed.
- Notes:
  - Daily usage is now `./start_host.sh` on host and `./start_robot.sh` on each robot after editing the local config files.
