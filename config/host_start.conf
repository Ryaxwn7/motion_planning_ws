# Host startup config for start_host.sh.
# This file is sourced by bash. Keep it shell-compatible.

START_ROSCORE=true
LAUNCH_MAP_SERVER=true
AUTO_START_GATHER=true
ROSCORE_WAIT=8.0

# Set these to your actual host network values if needed.
ROS_MASTER_URI_VALUE="${ROS_MASTER_URI:-http://127.0.0.1:11311}"
ROS_IP_VALUE="${ROS_IP:-}"

HOST_LAUNCH_ARGS=(
  "agent_number:=4"
  "shape_type:=rectangle"
  "shape_source:=mat"
  "shape_scale:=1.0"
  "auto_shape_heading:=true"
  "replan_mode:=event"
)
